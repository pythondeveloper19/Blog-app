$("#comment").html("#{escape_javascript(render :partial => 'posts/comment',  :locals => {:post_id => params[:post_id], :comments => @comments} )}");
$("#comment_size").text('#{!@comments.blank? ? @post.comments.size : 0}');
